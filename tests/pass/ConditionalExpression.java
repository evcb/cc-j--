package pass;

import jdk.internal.agent.resources.agent;
import jdk.internal.agent.resources.agent_ja;

public class ConditionalExpression {
    public boolean express(boolean a) {
        return a ? true : false;
    }
}